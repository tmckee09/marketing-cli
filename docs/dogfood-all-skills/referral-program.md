# /referral-program — design

## Loop
Builder shares `mktg init` success screenshot → friend installs → both get a "skill pack badge" in community wall (social, not paid).

## Mechanics
- No paid credits (OSS)
- Reward = featured dogfood story on README wall
