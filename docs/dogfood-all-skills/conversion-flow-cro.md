# /conversion-flow-cro — activate → first value

## Funnel
Install → `mktg init` → populate 2 brand files → `/cmo` → first artifact

## Fixes
- Postinstall already copies skills — celebrate that in first-run message
- Wizard (`mktg-setup`) should be the default `/cmo` first-run path
- Success moment: `mktg status` shows non-template voice + audience
