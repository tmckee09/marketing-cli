# /free-tool-strategy — wedge idea

**Tool:** "Brand Memory Diff" — paste two session transcripts, get a `voice-profile.md` draft.

Why it markets mktg: output is a brand file that drops straight into `mktg brand update`.
