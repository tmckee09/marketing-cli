# /landscape-scan — ecosystem notes

- Coding agents are the new operator runtime
- Skill repos proliferate; few ship CLI + memory + CI-enforced DX
- AXI shows principled CLIs beating MCP on cost/turns
- Exa vs Firecrawl: search vs known-URL

Claims blacklist honored in `brand/landscape.md`.
