# /create-skill — proposal

## New skill idea: `dogfood-loop`
Description: Run the product's own skills against itself and emit a scorecard.
Reads: brand/*, skills-manifest.json
Writes: docs/dogfood-*/SUMMARY.md
Would automate this exact session.
