# Universal Copy Bank — mktg

Reusable blocks. Mix per surface; don't paste identical walls everywhere.

## Name
mktg · marketing-cli

## One-liner
Agent-native marketing playbook — 64 skills, 6 agents, brand memory that compounds.

## Elevator (2 sentences)
Install one CLI and your coding agent gets a versioned marketing playbook: skills, research agents, and `brand/` files that persist across sessions. `/cmo` orchestrates the work; Studio is for the human — same npm package.

## Hero headline options
1. Your coding agent already ships product. Now give it a marketing playbook.
2. Marketing that compounds — for agents that ship.
3. CLI for the agent. Studio for the human.

## Hero subheads
- Install one CLI. 64 skills. 6 research agents. Brand memory that compounds.
- Skills work at zero context. Brand memory sharpens — never gates.
- JSON when piped. `--dry-run` before mutations. Playbooks you can test.

## CTAs
| Primary | Secondary |
|---------|-----------|
| `npm i -g marketing-cli` | View on GitHub |
| Run `mktg init` | Open docs |

## Feature bullets (pick 4)
- 64 versioned skills (manifest-backed, drop-in)
- 6 agents for parallel research and review
- Persistent `brand/` memory with progressive enhancement
- `/cmo` orchestration + local `mktg studio`
- Agent DX: JSON, dry-run, field filtering, CI-enforced score

## Anti-positioning (use in FAQ)
- Not a social scheduler SaaS
- Not a paste-once prompt pack
- Not "AI that writes your LinkedIn" as the whole product

## Objection → answer
| Objection | Answer |
|-----------|--------|
| I already use ChatGPT | Chats don't keep `brand/voice-profile.md` for next Tuesday. Files do. |
| Is this Typefully? | No. Schedulers can be adapters. mktg is the playbook. |
| Need API keys first? | No for writing at L0. Keys unlock distribution adapters. |
| Will you gate skills later? | Core skills stay OSS. Paid (if any) is sync/collab convenience. |

## Social bio (160)
Agent-native marketing playbook CLI. 64 skills · brand memory · /cmo. npm i -g marketing-cli

## Footer
CLI for the agent. Studio for the human. · MIT-clean · Built for Claude Code / Cursor builders
