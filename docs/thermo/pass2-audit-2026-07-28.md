# Thermo pass 2 — whole-repo audit (2026-07-28)

After merging PR #53, ran **30 parallel Grok 4.5** thermo-nuclear-code-quality-review partitions (3 waves of 10). Criteria: attached thermo-nuclear-code-quality-review skill. Every partition audited its file set line-by-line.

## Pre-merge E2E (PR #53)

| Suite | Result |
|---|---|
| `bun test` (CLI) | 3445 pass / 0 fail |
| `bun run --cwd studio test` | 226 pass / 0 fail |
| CI after `normalizeSignalRow` fix | green |
| Merge | #53 → `main` @ `ed52e25` |

## Pass-2 applied (this branch)

| Change | Impact |
|---|---|
| `src/core/project-assess.ts` | Deduped status/plan/doctor brand assessor |
| CLI `--fields` single choke point | Removed double filter in `formatOutput` |
| Studio content + publish routes | `server.ts` 2571 → **1946** |
| Postiz split + shared adapter finalize | `postiz.ts` 673 → client/markers/media/adapter/admin |

## Consensus remaining blockers (not all fixed this PR)

1. Finish `studio/server.ts` route table (still ~1.9k; dual wrapRoute/legacy stacks)
2. Shared brand registry CLI→Studio (freshness vocab `current` vs `fresh`)
3. Collapse `mktg dashboard` façade toward status/plan/Pulse
4. Single Postiz client for Studio (still mirrors CLI)
5. Verify-runner core so ship-check stops re-spawning verify
6. Shared field-projection kernel Studio↔CLI
7. OpenSEO: fake upstream on 15 skills; collapse 7 workflow skills; `seo-index.md` for `/cmo`
8. Test harnesses: `tests/helpers/cli.ts` + kill soft-skips; studio e2e one boot stack
9. Skills over 500 lines (`positioning-angles` 502, `landscape-scan` 500)
10. Manifest ↔ frontmatter drift CI; SCHEMA triplication

## Partition index

See `docs/thermo/pass2-partitions.md` (P1–P30).
