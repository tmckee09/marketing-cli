# SEO outline: agent marketing CLI

## Target page
`/blog/agent-native-marketing-cli` (or docs site)

## Primary keyword
agent marketing cli

## Outline
1. The session amnesia problem (agents forget brand)
2. What an agent-native playbook looks like (skills + memory + orchestrator)
3. Walkthrough: `npm i -g marketing-cli` → `mktg init` → `/cmo`
4. `/cmo` vs `/axi` (marketing vs tool-interface routing)
5. Proof: skill counts, DX axes, dry-run/JSON contract
6. CTA: install + first brand files

## Internal links
- README quickstart
- CONTEXT.md agent contract
- axi.md cross-link for tool ergonomics story
