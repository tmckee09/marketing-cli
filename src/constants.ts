// Canonical URLs. Import these everywhere instead of hardcoding the repo slug.
// If the repo is ever renamed again, this is the only file that changes.

export const GITHUB_REPO_URL = "https://github.com/MoizIbnYousaf/marketing-cli";
export const GITHUB_REPO_ISSUES_URL = `${GITHUB_REPO_URL}/issues`;
export const GITHUB_REPO_BLOB_MAIN = `${GITHUB_REPO_URL}/blob/main`;
