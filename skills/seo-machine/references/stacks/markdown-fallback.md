# Stack adapter — Markdown fallback (universal)

When the framework isn't directly supported, emit content as markdown files with structured frontmatter. The user wires them into their templating layer once; the skill keeps producing markdown for every phase after.

## File layout (universal)

```
content/seo/
├── alternatives/
│   ├── hootsuite.md
│   ├── buffer.md
│   └── ...
├── for/
│   ├── agencies.md
│   ├── social-listening.md
│   └── ...
├── compare/
│   ├── buffer-vs-hootsuite.md
│   └── ...
└── playbooks/
    ├── b2b-social-media-strategy.mdx
    └── ...
```

## Frontmatter schemas

### `content/seo/alternatives/<slug>.md`

```yaml
---
slug: hootsuite
competitor_name: Hootsuite
meta_title: "Hootsuite Alternative — ExampleCo (free, $25/mo Pro)"
meta_description: "Looking for a Hootsuite alternative? ExampleCo is the lightweight social monitoring tool with a free tier — no $249/mo Enterprise pricing."
canonical: "https://example.com/alternatives/hootsuite"
hero_eyebrow: "Hootsuite alternative"
hero_h1: "Looking for a Hootsuite alternative?"
hero_lede: "..."
table_h2: "Best Hootsuite alternatives in 2026"
table_lede: "..."
comparison_rows:
  - feature: "Free plan"
    yours: { state: yes,  note: "Free forever for 1 X account" }
    theirs: { state: no, note: "Discontinued in 2023" }
  - feature: "Starting price"
    yours: { state: text, note: "$25/mo Pro" }
    theirs: { state: text, note: "$249/mo Professional" }
  # ... ≥10 rows total
switch_reasons:
  - icon: bolt
    title: "Reply in 30 seconds, not 5 minutes"
    body: "..."  # ≥40 words
  # ... 4 total
honesty_rows:
  - feature: "Multi-platform scheduling"
    body: "..."  # ≥30 words
  # ... 3-4 total
faqs:
  - question: "Why did Hootsuite end its free plan?"
    answer: "..."  # ≥40 words
  # ... 4-6 total
cta_h2: "..."
cta_lede: "..."
related_internal_links:
  alternatives: [buffer, sprout-social]
  features: [twitter-monitoring]
  tools: [reply-templates]
schemas:
  - FAQPage
  - SoftwareApplication
  - BreadcrumbList
---

(Body is optional with this shape — the frontmatter contains all rendered data.)
```

### `content/seo/for/<slug>.md`

Same idea — full `ForData` shape as frontmatter:

```yaml
---
slug: agencies
name: Agencies
meta_title: "Social media tools for agencies — ExampleCo"
hero_eyebrow: "For agencies"
hero_h1: "Social media monitoring built for agencies"
hero_lede: "..."
pain_h2: "The agency reality"
pains:
  - title: "..."
    body: "..."
solution_h2: "..."
solutions:
  - icon: bolt
    title: "..."
    body: "..."
faqs: [...]
related_internal_links:
  features: [twitter-monitoring, brand-monitoring]
  tools: [hashtag-generator, share-of-voice]
  siblings: [freelancers]
---
```

### `content/seo/playbooks/<slug>.mdx`

Playbooks are different — frontmatter holds metadata, body holds the 2,500+ word prose:

```mdx
---
slug: b2b-social-media-strategy
title: "B2B social media strategy: a 2,500-word playbook"
meta_title: "B2B social media strategy — ExampleCo playbook"
meta_description: "..."
date_published: 2026-01-15
date_modified: 2026-01-15
read_time_min: 18
related_internal_links:
  features: [twitter-monitoring, brand-monitoring, linkedin-inbox]
  tools: [reply-templates, share-of-voice]
  for_pages: [b2b-saas, agencies]
  alternatives: [hootsuite]
---

# B2B social media strategy: a 2,500-word playbook

## Section 1 — the problem framing

Body content here.

## Section 2 — the mental model
...
```

## How the user wires this into their template

After the skill writes its first markdown file, hand off with this note:

> The skill writes content as markdown to `content/seo/`. You'll need to wire your framework to read these files and render them. Common patterns:
>
> - **Hugo:** drop the files in `content/seo/alternatives/`, use a custom layout per content type
> - **Jekyll:** use collections (`_alternatives/`, etc.) configured in `_config.yml`
> - **Eleventy:** use a `_data/` directory or collection front-matter
> - **Gatsby:** source-filesystem plugin + `createPages` in `gatsby-node.js`
> - **VitePress/Docusaurus:** treat as MDX pages
> - **WordPress:** use a custom theme that parses these or import as posts
>
> Once wired, every subsequent phase just adds another file in the same directory. The skill handles the data; you handle the rendering.

## Sitemap (universal)

Most SSG frameworks generate sitemaps automatically once content is in the right collection. If yours doesn't:

```
content/seo/sitemap-data.yaml         # generated by skill, alphabetical
```

Then have your build step emit `/sitemap.xml` from this file. Example shape:

```yaml
last_modified: 2026-MM-DD
urls:
  - loc: https://example.com/alternatives/hootsuite
    changefreq: monthly
    priority: 0.8
  - loc: https://example.com/for/agencies
    changefreq: monthly
    priority: 0.8
  # ...
```

## File-write checklist for any phase (markdown mode)

- [ ] New markdown file at the right `content/seo/<pattern>/<slug>.md` path
- [ ] Frontmatter validates against the pattern schema
- [ ] Sibling files updated to add the new slug under `related_internal_links` (≥2 spots)
- [ ] `sitemap-data.yaml` updated if used
- [ ] User's build step ran successfully (rebuild + verify HTML output)
- [ ] HTML output: `grep -c '<h1' build-output/alternatives/<slug>/index.html` — must be >0
- [ ] `docs/seo-machine.md` tracker row updated in same commit
